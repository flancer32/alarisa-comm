# @flancer32/alarisa-comm

Main shared communication package for Alarisa applications.

The package owns transport-independent contracts and communication semantics under the `Alarisa_Comm_` TeqFW namespace. It is orthogonal to `@flancer32/alarisa-back` and is intended for both the self-hosted server and client applications.

The embedded cognitive context is in `ctx/`.
