# Source

## Purpose

Contains TeqFW ECMAScript modules in the `Alarisa_Comm_` namespace.

## Boundary

Keep shared server/client communication contracts and implementations here. This package is orthogonal to `@flancer32/alarisa-back`; platform-specific presentation and server authority remain outside it.

Do not add placeholder modules merely to populate the namespace.
